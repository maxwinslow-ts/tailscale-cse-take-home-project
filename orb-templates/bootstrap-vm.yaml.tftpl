#cloud-config
package_update: true
package_upgrade: true

packages:
  - ca-certificates
  - docker.io
  - docker-compose-v2

runcmd:
  - mkdir -p /opt/stack
  - systemctl enable docker
  - systemctl start docker