[defaults]
inventory = inventory.yml
host_key_checking = False
timeout = 60
interpreter_python = /usr/bin/python3

# OrbStack has a built-in SSH server on the `orb` host.
# Ansible connects via: ssh root@<machine>@orb
# Key: ~/.orbstack/ssh/id_ed25519 (auto-generated by OrbStack)
